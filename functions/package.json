{
  "name": "hajobjah-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@google/genai": "^1.7.0",
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  }
}
